@page "/grid"
@inject SimulationService Sim
@inject IJSRuntime JS
@using System.Timers
@using Web.Data
@using Web.Services
@using Web.Services.Scenarios
@implements IDisposable

<PageTitle>Електромережа - Лабораторія MPC</PageTitle>

<div class="visualizer-container">
    <div class="simulation-area">
        <div class="simulation-header">
            <h1 class="simulation-title">Розумна Електромережа</h1>
        </div>

        <div class="canvas-wrapper">
            <canvas id="gridCanvas"></canvas>
        </div>

        <div class="control-buttons">
            <div class="button-group">
                <button class="btn @(_isRunning ? "btn-danger" : "btn-success")" @onclick="ToggleSim">
                    @(_isRunning ? "Стоп" : "Почати день")
                </button>
                <button class="btn btn-primary" @onclick="RunSingleStep">Крок (+@Sim.Dt.ToString("0.##") год)</button>
                <button class="btn btn-secondary" @onclick="Reset">Скинути</button>
            </div>
        </div>
    </div>

    <div class="control-panel">
        <div class="panel-header">
            <h2 class="panel-title">Керування</h2>
        </div>

        <div class="panel-content">
            @if (Context != null)
            {
                <section class="control-section">
                    <h3 class="section-title">Середовище</h3>

                    <div class="control-group">
                        <label class="control-label">Пора року</label>
                        <select class="form-control" @bind="Context.Season" @bind:after='() => ToggleConfig("SetSeason:" + Context.Season)'>
                            <option value="Summer">Літо (Макс Сонця)</option>
                            <option value="Spring">Весна (Багато Сонця)</option>
                            <option value="Autumn">Осінь (Мало Сонця)</option>
                            <option value="Winter">Зима (Мін Сонця)</option>
                        </select>
                    </div>

                    <div class="control-group">
                        <label class="control-label">
                            <span>Крок симуляції (dt)</span>
                            <span class="control-value">@Sim.Dt.ToString("F3") год</span>
                        </label>
                        <select class="form-control" value="@Sim.Dt" @onchange="OnDtChanged">
                            <option value="1.0">1 година</option>
                            <option value="0.5">30 хвилин</option>
                            <option value="0.3333333">20 хвилин</option>
                            <option value="0.25">15 хвилин</option>
                            <option value="0.1666667">10 хвилин</option>
                        </select>
                    </div>

                    <div class="toggle-row" style="margin-top: 1rem;">
                        <label class="toggle-label" for="cloudSwitch">Хмарна погода</label>
                        <div class="form-switch">
                            <input class="form-check-input" type="checkbox" id="cloudSwitch"
                                   checked="@Context.IsCloudy"
                                   @onchange='() => ToggleConfig("ToggleCloudy")'>
                        </div>
                    </div>

                    <div class="control-group">
                        <label class="control-label">
                            <span>Статус мережі</span>
                            <span class="control-value" style="color: @(Context.IsBlackout ? "#EF4444" : "#10B981")">
                                @(Context.IsBlackout ? "Блекаут" : "Онлайн")
                            </span>
                        </label>
                        <button class="btn btn-sm btn-outline-danger" style="width: 100%"
                                @onclick='() => ToggleConfig("ToggleBlackout")'>
                            @(Context.IsBlackout ? "Вимкнути блекаут" : "Симулювати блекаут")
                        </button>
                    </div>

                    <div class="control-group">
                        <label class="control-label">
                            <span>Похибка прогнозу</span>
                            <span class="control-value">@((Context.ForecastError * 100).ToString("F0"))%</span>
                        </label>
                        <input type="range" class="form-range" min="0.0" max="1.0" step="0.05"
                               @bind-value="Context.ForecastError"
                               @bind-value:event="oninput"
                               @onmouseup='() => ToggleConfig("Refresh")' />
                    </div>
                </section>

                <section class="control-section">
                    <h3 class="section-title">Інфраструктура</h3>

                    <div class="control-group">
                        <label class="control-label">
                            <span>Сонячні панелі</span>
                            <span class="control-value">@(Context.HasSolar ? "Готові" : "Не готові")</span>
                        </label>
                        <button class="btn btn-sm btn-secondary" style="width: 100%"
                                @onclick='() => ToggleConfig("ToggleSolar")'>
                            @(Context.HasSolar ? "Вимкнути панелі" : "Увімкнути панелі")
                        </button>
                    </div>

                    <div class="control-group">
                        <label class="control-label">
                            <span>Дизель-генератор</span>
                            <span class="control-value">@(Context.HasGenerator ? "Готовий" : "Не готові")</span>
                        </label>
                        <button class="btn btn-sm btn-secondary" style="width: 100%"
                                @onclick='() => ToggleConfig("ToggleGen")'>
                            @(Context.HasGenerator ? "Вимкнути генератор" : "Увімкнути генератор")
                        </button>
                    </div>


                    <div class="control-group">
                        <label class="control-label">Ємність батареї (кВт·год)</label>
                        <input type="number" class="form-control" step="10"
                               @bind-value="Context.BatteryCapacity" @bind-value:event="oninput" />
                    </div>

                    <div class="control-group">
                        <label class="control-label">
                            <span>Ефективність (ККД)</span>
                            <span class="control-value">@((Context.BatteryEfficiency * 100).ToString("F0"))%</span>
                        </label>
                        <input type="range" class="form-range" min="0.0" max="1.0" step="0.01"
                               @bind-value="Context.BatteryEfficiency" @bind-value:event="oninput" />
                    </div>
                </section>

                <section class="control-section">
                    <h3 class="section-title">Ринок та Стратегія</h3>

                    <div class="sub-control-section">
                        <div class="toggle-row">
                            <label class="toggle-label" for="peakSwitch">Піковий тариф</label>
                            <div class="form-switch">
                                <input class="form-check-input" type="checkbox" id="peakSwitch"
                                       @bind="Context.UsePeakPricing"
                                       @bind:after='() => ToggleConfig("Refresh")'>
                            </div>
                        </div>

                        @if (Context.UsePeakPricing)
                        {
                            <div class="control-group">
                                <label class="control-label">
                                    <span>Ціна (День / Пік)</span>
                                </label>
                                <div style="display: flex; gap: 0.5rem;">
                                    <input type="number" class="form-control" step="0.01" @bind="Context.BasePrice"
                                           @bind:after='() => ToggleConfig("Refresh")' placeholder="Base" />
                                    <input type="number" class="form-control" step="0.01" @bind="Context.PeakPrice"
                                           @bind:after='() => ToggleConfig("Refresh")' placeholder="Peak" />
                                </div>
                            </div>

                            <div class="control-group">
                                <label class="control-label">Години пік</label>
                                <div style="display: flex; align-items: center; gap: 0.5rem; font-size: 0.8rem; color: var(--text-light);">
                                    <input type="number" class="form-control" min="0" max="23" @bind="Context.PeakStartHour"
                                           @bind:after='() => ToggleConfig("Refresh")' />
                                    <span>до</span>
                                    <input type="number" class="form-control" min="0" max="23" @bind="Context.PeakEndHour"
                                           @bind:after='() => ToggleConfig("Refresh")' />
                                </div>
                            </div>
                        }
                    </div>

                    <div class="control-group" style="margin-top: 1rem;">
                        <label class="control-label">
                            <span>Коефіцієнт продажу</span>
                            <span class="control-value">@Context.SellPriceRatio.ToString("F2")</span>
                        </label>
                        <input type="range" class="form-range" min="0.0" max="1.0" step="0.1"
                               @bind-value="Context.SellPriceRatio" @bind-value:event="oninput" />
                    </div>

                    <div class="control-group">
                        <label class="control-label">
                            <span>Згладжування (Анти-Пік)</span>
                            <span class="control-value">@Context.GridSmoothing.ToString("F3")</span>
                        </label>
                        <input type="range" class="form-range" min="0.0" max="0.2" step="0.005"
                               @bind-value="Context.GridSmoothing" @bind-value:event="oninput" />
                        <div style="font-size: 0.7rem; color: var(--text-lighter); margin-top: 0.25rem;">
                            Більше = плавніше, Менше = різкіше (економніше)
                        </div>
                    </div>

                    <div class="control-group">
                        <label class="control-label">Вартість пального ($/кВт·год)</label>
                        <input type="number" class="form-control" step="0.01" min="0"
                               @bind-value="Context.FuelCost" @bind-value:event="oninput"
                               @onchange='() => ToggleConfig("Refresh")' />
                    </div>

                    <div class="control-group">
                        <label class="control-label">
                            <span>Початковий заряд</span>
                            <span class="control-value">@Context.InitialBatteryPercent.ToString("F0")%</span>
                        </label>
                        <input type="range" class="form-range" min="0" max="100" step="10"
                               @bind-value="Context.InitialBatteryPercent" @bind-value:event="oninput" />
                    </div>
                </section>

                @if (_visuals != null)
                {
                    <div class="telemetry-panel">
                        <h3 class="telemetry-title">Результати</h3>
                        <div class="telemetry-grid">
                            <div class="telemetry-item">
                                <div class="telemetry-label">Час</div>
                                <div class="telemetry-value">@((_visuals.CurrentStep * Sim.Dt).ToString("F1")) год</div>
                            </div>
                            <div class="telemetry-item">
                                <div class="telemetry-label">Заряд</div>
                                <div class="telemetry-value">@(_visuals.PlannedBattery.ElementAtOrDefault(_visuals.CurrentStep > 0 ? _visuals.CurrentStep - 1 : 0).ToString("F1")) кВт</div>
                            </div>
                            <div class="telemetry-item">
                                <div class="telemetry-label">Баланс</div>
                                <div class="telemetry-value" style="color: @(_visuals.CurrentCost > 0 ? "#F59E0B" : "#10B981")">
                                    $@(_visuals.CurrentCost.ToString("F2"))
                                </div>
                            </div>
                        </div>
                    </div>
                }

                <section class="control-section">
                    <h3 class="section-title">Експорт</h3>
                    <div class="control-group">
                        <button class="btn btn-sm btn-outline-primary" style="width: 100%" @onclick="DownloadData">
                            Завантажити результати (.csv)
                        </button>
                    </div>
                </section>
            }
        </div>
    </div>
</div>

@code {
    private DotNetObjectReference<GridVisualizer>? _objRef;
    private GridVisuals? _visuals;
    private Timer? _timer;
    private bool _isRunning = false;
    private bool _disposed = false;

    private GridScenario? Context => Sim.ActiveScenario as GridScenario;

    protected override async Task OnInitializedAsync()
    {
        Sim.LoadScenario("grid");
    }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender && !_disposed)
        {
            _objRef = DotNetObjectReference.Create(this);
            await JS.InvokeVoidAsync("initCanvasEvents", "gridCanvas", _objRef);
            await UpdateVisuals();
        }
    }

    private async Task ToggleSim()
    {
        if (_isRunning) Stop();
        else await Start();
    }

    private async Task Start()
    {
        if (_timer != null || _disposed || Context == null) return;

        // If already finished, reset first so Start always restarts
        if (_visuals != null && (_visuals.CurrentStep * Sim.Dt) >= 24.0)
        {
            await Reset();
        }

        double dtForTimer = Math.Max(0.01, Sim.Dt);
        int tickSpeed = (int)(500 * dtForTimer);
        if (tickSpeed < 50) tickSpeed = 50;

        _timer = new Timer(tickSpeed);
        _timer.Elapsed += async (s, e) =>
        {
            if (_disposed) { Stop(); return; }
            await InvokeAsync(async () => await Tick());
        };
        _timer.Start();
        _isRunning = true;
    }

    private void Stop()
    {
        _timer?.Stop();
        _timer = null;
        _isRunning = false;
    }

    private async Task Tick()
    {
        if (_disposed || Context == null) return;

        Sim.RunStep();
        await UpdateVisuals();

        double currentHour = _visuals.CurrentStep * Sim.Dt;
        if (currentHour >= 24.0)
        {
            Stop();
            if (!_disposed) StateHasChanged();
        }
    }

    private async Task RunSingleStep()
    {
        // If sim was finished, reset before running a single step so single-step can restart
        if (_visuals != null && (_visuals.CurrentStep * Sim.Dt) >= 24.0)
        {
            await Reset();
        }

        Stop();
        await Tick();
    }

    private async Task OnDtChanged(ChangeEventArgs e)
    {
        if (_disposed) return;
        if (e?.Value == null) return;

        if (double.TryParse(e.Value.ToString(), out var newDt) && newDt > 0)
        {
            Sim.Dt = newDt;

            // tell the scenario to regenerate at the new profile resolution
            Sim.HandleInteraction(0, 0, $"SetDt:{newDt}");

            await UpdateVisuals();
        }
    }

    private async Task ToggleConfig(string? mode)
    {
        if (mode != null) Sim.HandleInteraction(0, 0, mode);
        else Sim.HandleInteraction(0, 0, "Refresh");

        await UpdateVisuals();
    }

    private async Task Reset()
    {
        if (_disposed) return;
        Stop();
        Sim.Reset();
        await UpdateVisuals();
    }

    private async Task DownloadData()
    {
        if (Context == null || _disposed) return;
        string csv = Context.GetCsv();
        string fileName = $"grid_data_{DateTime.Now:yyyy-MM-dd_HH-mm-ss}.csv";
        await JS.InvokeVoidAsync("downloadCsv", fileName, csv);
    }

    private async Task UpdateVisuals()
    {
        if (_disposed) return;
        _visuals = Sim.GetCurrentVisuals() as GridVisuals;
        if (_visuals != null && !_disposed)
        {
            try { await JS.InvokeVoidAsync("drawGrid", "gridCanvas", _visuals); } catch { }
        }
        if (!_disposed) StateHasChanged();
    }

    [JSInvokable] public async Task Redraw() => await UpdateVisuals();
    [JSInvokable] public void HandleMouseDown(double x, double y) { }
    [JSInvokable] public void HandleMouseMove(double x, double y) { }
    [JSInvokable] public void HandleMouseUp() { }
    [JSInvokable] public void HandleRightClick(double x, double y) { }

    public void Dispose()
    {
        _disposed = true;
        Stop();
        try { ((IJSInProcessRuntime)JS).InvokeVoid("disposeCanvasEvents", "gridCanvas"); } catch { }
        _objRef?.Dispose();
    }
}
